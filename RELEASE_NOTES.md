# v0.3.8

本次更新完善了 GitHub 下载策略、运行环境重新部署以及无 NVIDIA 显卡电脑的 CPU 运行环境支持。

- 软件更新和 Runtime 下载可选择“国内加速（gh-proxy.org）”或“GitHub 原始连接”，选择会持久保存。
- 国内加速同时覆盖 Runtime 在线清单和 GitHub Release 分包，下载后仍强制校验 SHA-256。
- 增加“重新部署运行环境”，新环境完整下载、解压和验证成功前不会替换现有环境。
- 自动检测 NVIDIA 显卡；未检测到兼容显卡时推荐 CPU Runtime，并将训练与自动标注默认设为 CPU。
- 支持 CPU/CUDA Runtime 手动切换并持久保存选择。
- 新增独立的 Windows x64 PyTorch CPU Runtime，CPU 电脑无需下载 CUDA 环境。

---

# v0.3.7

修复首次安装运行环时可能长时停在“正在解压部署 1/7”的问题。

- Windows 改用独立的系统解压进程，避免 Electron 内部文件流在 DLL 写入时无限等待。
- 解压现在支持取消和 30 分钟超时，失败时会显示明确原因。
- 重试前自动清理上次未完成的 staging 目录，已下载并通过校验的分包仍会复用。
- 解压阶段的进度从已完成下载的 95% 继续显示，不再误显示为 0%。
- 本地版本高于 GitHub 已发布版本时，更新弹窗会分别显示“当前安装版本”和“线上最新正式版本”，不再把旧版本误标为当前版本。

---

# v0.3.6

本次更新重点提升分割模型训练的稳定性，以及训练日志的可读性。

## 数据集预检

- 支持识别目标检测与实例分割两种 YOLO 标注格式。
- 开始训练前检查分割多边形坐标是否位于有效范围内，提前提示具体标注问题。
- 分割数据集中混入目标检测标注时会阻止训练，避免训练过程中出现难以定位的 CUDA 异常。

## 分割训练

- 根据模型名称自动选择正确的训练任务，分割模型会使用分割训练模式。
- 优化离线环境下的 AMP 检查，优先使用随程序提供的模型文件，避免不必要的网络下载。

## 日志与兼容性

- 合并重复的警告和错误摘要，减少同一问题反复显示。
- 修正部分普通训练输出被误判为预测进度的问题。
- 训练子进程统一使用 UTF-8 输出，更好地支持中文及其他非 ASCII 路径。
