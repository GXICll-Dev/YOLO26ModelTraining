# v0.3.3

新增独立的 Windows 卸载程序。

- 保留 Windows“设置 → 应用 → 已安装的应用”中的标准卸载项。
- 安装目录新增真实的 `Uninstall.exe`，可直接双击卸载。
- `Uninstall.exe` 会调用 Squirrel 官方卸载流程，不是开始菜单快捷方式或脚本文件。
- 安装、软件更新或正常启动时都会自动检查并部署缺失的卸载程序。
- 独立 Runtime 和用户训练数据默认保留，重新安装软件后仍可复用。
